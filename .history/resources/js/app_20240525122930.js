import 'bootstrap';
import.meta.glob([
  '../images/**',
  '../fonts/**',
]);

function DarkMode()
{
    var bodyArray;

    bodyArray = document.getElementsByClassName("body-home-light");

    while (bodyArray.length) {
        bodyArray.
    }
}
