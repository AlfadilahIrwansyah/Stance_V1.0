import 'bootstrap';
import.meta.glob([
  '../images/**',
  '../fonts/**',
]);

$("")
    var bodyArray;

    bodyArray = document.getElementsByClassName("body-home-light");

    while (bodyArray.length) {
        bodyArray.
    }
}
